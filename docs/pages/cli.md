# CLI Commands

```
deploq serve              # Start webhook server
deploq deploy <project>   # Manual deploy (synchronous)
deploq init               # Generate deploq.yaml template
deploq validate           # Validate config
deploq version            # Print version
```
